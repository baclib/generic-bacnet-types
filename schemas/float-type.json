{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://baclib.github.io/float-type.json",
    "title": "BAClib Float Type",
    "description": "Defines a BAClib Float type with an optional constrains the allowed range of values.",
    "allOf": [
        {
            "$ref": "./common-type.json"
        },
        {
            "type": "object",
            "properties": {
                "base": {
                    "const": "float",
                    "description": "The base type. Must always be `float` for this schema."
                },
                "range": {
                    "type": "object",
                    "description": "Optional range constraint for allowed float values.",
                    "properties": {
                        "minimum": {
                            "type": "number",
                            "description": "The minimum allowed value. If maximum is present, minimum must be less than or equal to maximum."
                        },
                        "maximum": {
                            "type": "number",
                            "description": "The maximum allowed value. If minimum is present, maximum must be greater than or equal to minimum."
                        }
                    },
                    "anyOf": [
                        {
                            "required": [
                                "minimum"
                            ]
                        },
                        {
                            "required": [
                                "maximum"
                            ]
                        }
                    ],
                    "additionalProperties": false
                }
            }
        }
    ]
}