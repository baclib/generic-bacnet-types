{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://baclib.github.io/common-type.json",
    "title": "Common Type",
    "type": "object",
    "properties": {
        "base": {
            "type": "string",
            "description": "The name of the base type."
        },
        "xyz-base": {
            "$ref": "./type-name.json",
            "description": "The name of the type which is the base for this definition."
        },
        "series": {
            "description": "Indicates if this is an array or list. If absent, it should be interpreted as false.",
            "oneOf": [
                {
                    "type": "boolean"
                },
                {
                    "$ref": "./length-constraint.json"
                }
            ]
        },
        "xyz-length": {
            "$ref": "./length-constraint.json",
            "description": "Optional length constraint for string types."
        },
        "xyz-range": {
            "$ref": "./range-constraint.json",
            "description": "Optional range constraint for numeric types."
        }
    },
    "required": [
        "base"
    ]
}