{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://baclib.github.io/schemas/if-sequence.json",
    "title": "BAClib If-Sequence Applicator",
    "description": "Conditional schema that applies when the base type is 'sequence'. Defines the 'fields' property which contains at least one named field, each with a type and optional context number.",
    "type": "object",
    "if": {
        "properties": {
            "base": {
                "const": "sequence"
            }
        }
    },
    "then": {
        "properties": {
            "fields": {
                "type": "array",
                "description": "Array of sequence fields. Each entry must have a name and a type.",
                "minItems": 1,
                "items": {
                    "type": "object",
                    "properties": {
                        "alias": {
                            "type": "string",
                            "description": "An optional unique alias for the field."
                        },
                        "name": {
                            "type": "string",
                            "description": "The unique name of the field."
                        },
                        "context": {
                            "type": "integer",
                            "minimum": 0,
                            "description": "The context number of the field. Must be unique within the set."
                        },
                        "description": {
                            "type": "string",
                            "description": "An optional human-readable description for the field."
                        },
                        "type": {
                            "description": "The type of the field, either a type name or a reference to type traits.",
                            "oneOf": [
                                {
                                    "$ref": "./type-name.json"
                                },
                                {
                                    "$ref": "./type-traits.json"
                                }
                            ]
                        },
                        "optional": {
                            "type": "boolean",
                            "default": false,
                            "description": "Indicates if the field is optional. Defaults to false."
                        }
                    },
                    "required": [
                        "name",
                        "type"
                    ],
                    "additionalProperties": false
                }
            }
        },
        "required": [
            "fields"
        ]
    }
}