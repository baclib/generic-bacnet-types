{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://baclib.github.io/unsigned-type.json",
    "title": "BAClib Unsigned Type",
    "description": "Defines a BAClib Unsigned type with an optional constrains the allowed range of values.",
    "allOf": [
        {
            "$ref": "./common-type.json"
        },
        {
            "type": "object",
            "properties": {
                "base": {
                    "const": "unsigned",
                    "description": "The base type. Must always be `unsigned` for this schema."
                },
                "range": {
                    "type": "object",
                    "description": "Optional range constraint for allowed unsigned values.",
                    "properties": {
                        "minimum": {
                            "type": "integer",
                            "minimum": 0,
                            "description": "The minimum allowed value. If maximum is present, minimum must be less than or equal to maximum."
                        },
                        "maximum": {
                            "type": "integer",
                            "minimum": 0,
                            "description": "The maximum allowed value. If minimum is present, maximum must be greater than or equal to minimum."
                        }
                    },
                    "anyOf": [
                        {
                            "required": [
                                "minimum"
                            ]
                        },
                        {
                            "required": [
                                "maximum"
                            ]
                        }
                    ],
                    "additionalProperties": false
                }
            },
            "additionalProperties": false
        }
    ]
}