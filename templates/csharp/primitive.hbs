/// <summary>
/// Represents the primitive {{bacnetName}} as defined in ANSI/ASHRAE 135-2024 Clause 21.
/// </summary>
public struct {{typeName}}
{
    private readonly object _value;

    public {{typeName}}(object value)
    {
        _value = value;
    }

    public object Value => _value;

    public static implicit operator {{typeName}}(object value) => new {{typeName}}(value);
    public static explicit operator object({{typeName}} wrapper) => wrapper._value;

    public override string ToString() => _value?.ToString() ?? string.Empty;
}
